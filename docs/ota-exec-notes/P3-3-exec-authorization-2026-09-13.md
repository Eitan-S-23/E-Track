# P3-3 集中执行授权记录（P3-3-EXEC-AUTH-20260913）—— 2026-09-13

> **性质与来源**：本文件是实现会话对用户 2026-09-13 集中执行授权消息
> （标识 `P3-3-EXEC-AUTH-20260913`，五节）的**整理记录**，供独立复核
> 会话与后续审计对照。除明确标注「原文」的引号片段外，文字为按原消息
> 五节结构与全部操作性条款的忠实整理，非逐字转录；如与各执行文档
> （操作单第五版、恢复方案 v5、服务文档第五轮修订、合同 v3）引用的
> 原文片段存在出入，以更接近原始消息语境者为准并回改本文件。
>
> **状态**：授权生效中。已授权但尚未执行的项目在各文档中统一标注
> 「已授权，待前检/执行」。

## 第一节：工作方式与责任

- 允许在现有独占 admission worktree、`dev/flutter/apk/p3-3-admission`
  分支内进行范围内整改、宿主自测、提交和推送，沿用既有 GitHub 身份
  （Eitan-S-23）。不得合并、推主干、创建 Release/tag、修改生产发布
  渠道或凭据。
- 技术实现、工具参数、日志整理、正常故障定位由 agent 处理，不再当作
  用户决策；发现技术问题先在授权范围内修复、自测，再交独立复核。
- 授权未参与实现的独立验收会话核对实际提交、产物、设备、端点和操作
  单，满足条件后办理合同冻结及执行前检。实现 agent 不得自行冒充独立
  验收者或伪填审批人。

## 第二节：HTTPS 路线直接确定（Quick Tunnel）

- 本轮优先采用一个测试专用 Quick Tunnel，不再要求用户提供域名、DNS
  API token 或证书策略。
- 用户明确否决「随机 URL 与 APK 注入必然冲突」的排除理由：先启动隧道
  取得实际 HTTPS 地址，完成宿主端到端检查，再将该地址注入唯一一次
  受验 APK 构建，并绑定进合同。
- 授权创建并运行本轮短期公网隧道，只暴露本服务的 latest/download
  端点及两份已绑定测试 ETU；不得暴露其他本机服务、文件、日志或管理
  接口；开放窗口最长 8 小时，本轮结束或到期即关闭。
- App 到公网入口必须保持公共可信 HTTPS；隧道到本机服务可走
  127.0.0.1 HTTP，不开放局域网监听，不改变 App 的证书或 HTTPS 校验。
- 本路线不需要 adb reverse，不要沿用旧方案去创建或删除无关映射。
- toy→真包切换只重启本机后端，维持同一隧道进程和公网地址；地址改变
  或隧道退出时暂停相关阶段，不偷偷换地址、不自动消耗额外 APK 构建。
- Quick Tunnel 不使用用户的 Cloudflare 账号，不读取旧 token。若确实
  不可用需改用 Named Tunnel/DNS/其他账号资源，再集中提出最小替代
  方案；必须先按用户要求发起浏览器鉴权，随后核对 account ID、资源
  归属及旧环境 token 是否覆盖新登录。不得通过聊天索取或输出
  token/cookie。

## 第三节：设备恢复授权

- 修正后的精确操作单经独立复核通过后，授权 REC0-REC7 执行一次。
- REC0 的 Boot 全区备份和 REC2 的清空前 BCB 原始 128B 备份必须保留，
  不允许裁剪。
- 允许测试 Boot 烧录及原样恢复，Flash 边界限定 0x08000000-0x08004FFF。
- 允许恢复命令所需 RAM 控制块写入，限定 0x20057E00-0x20057FFF。
- 明确授权通过既有 CLEAR_BCB 清空 EEPROM 0x00-0x7F，并由既有 Boot
  状态机根据有效 App 重建 CONFIRMED。
- 恢复阶段不授权 App 烧录、QSPI 槽写入、断电注错或其他测试 opcode。
- 正常路径最多 10 个 J-Link 命令会话、2 个 RTT logger、6 次主动复位；
  另预留最多 2 个仅用于必要身份/RTT 签名核对的命令会话，不授权额外
  主动复位或持久写入；每个 logger 最长 120 秒。
- FR4 与 REC5 是替代关系，只消费一次，先自行消除文档中「替代却又
  增加复位/会话」的矛盾（恢复方案 v5 已消除：FR4 备用会话替代 S5，
  复位恒 6、命令会话恒 10）。
- 首次启用测试 Boot 前，必须在其第一次启动执行命令之前完成实际 RAM
  magic 失效及验证（恢复方案 v5 S1a 已落实）。
- 允许在备份有效、恢复前提仍成立时将 REC6 用于失败后的生产 Boot
  安全收尾，与成功路径共用同一次额度，不额外增加；不得自动重复
  CLEAR_BCB、直接写回 BCB 原字节或追加烧录。
- 计划外 nRESET 仍按既定阈值处理（单步 ≥2 次或全程累计 >2 次即停止）。

## 第四节：APK 与 O 序列授权

- 保留现有同一笔 build-only 1 次额度，不重复追加。HTTPS 地址、安装
  策略和开发自测齐备后使用；publish_release=false、
  replace_existing_release=false，Pages/Release 不得执行。
- 签名兼容策略必须在这次构建前解决：若无法保持既有安装签名，允许为
  本轮使用显式启用的专用并存 applicationId（例如
  com.wen.gaia.gaia.p33acceptance）并写入合同；不得改变生产默认包名，
  不卸载旧应用、不清数据。
- 同一受验 APK 必须贯穿安装与两个升级闭环。
- 恢复完成、服务与资产准入、合同冻结和 NOT_RUN 前检通过后，授权指定
  测试手机 10ADA4197U001CK 与已绑定目标板执行 O1 安装/启动一次
  5 分钟、O2 真实 latest/下载观测一次 10 分钟、O3 toy 30201 完整闭环
  一次 30 分钟、O4 真包 30202 完整闭环一次 30 分钟、O5 最终身份核对
  一次 5 分钟。
- 包括该 App 必要的正常 BLE 权限授予、目标板连接，以及正常 OTA 所需
  的候选/备份槽、App Flash、BCB 更新和固件重启；不包括通过 J-Link
  直接刷 App 或注错。
- O3 的重启后新连接 GET_INFO 版本和完整 raw SHA 匹配后，才允许激活
  真包并进入 O4。
- 失败留证，不盲重试、不互借历史 A1/B1-M 额度。

## 第五节：执行边界与停止条件

- 安全准备、HTTPS 检查和 APK 构建可独立推进，不必先操作设备；准备
  充分后再安排恢复和升级，避免设备长期停留在测试状态。
- 全部可控输出、工具下载、缓存、认证文件和证据先做路径预检，优先
  收敛到活动 worktree；不全局安装工具、不修改全局 PATH，不擅改项目
  外配置；只管理本次创建的进程和资源，清理与归档包含在本次授权内。
- 只在以下情况集中问用户一次：
  1. 必须扩大资源/设备/写入边界；
  2. 增加费用或配额；
  3. 修改验收门槛或目标版本；
  4. 使用无法唯一确认的账号资源；
  5. 需要不可推断的用户输入。
- 已获条件授权但尚未执行的项目，应标为「已授权，待前检/执行」，
  不得继续笼统写「全部未授权」。
- 交付一次完整结果：实际执行项、独立验收结论、证据、剩余额度及
  清理状态。

## 与各执行文档的映射

| 授权节 | 承接文档 |
| --- | --- |
| 第一节 | 本文件 + 各文档「已授权，待前检/执行」口径 |
| 第二节 | 服务文档第五轮修订 §5（Quick Tunnel）/§6（D1-D5）；操作单 §2 P1/D4/D5 |
| 第三节 | 恢复方案 v5（§3 S1a-S6、§7 额度总账、§8 RTT 纪律）；操作单 §3 |
| 第四节 | build.gradle.kts + build.yml（suffix 注入）；操作单 §1/§4/§7；合同 v3 CMD-RELEASE-BUILD/CMD-APK-INSTALL |
| 第五节 | 操作单 §2 顺序说明与 §8 边界声明；服务文档 §6 8h 窗口与清理判据 |
