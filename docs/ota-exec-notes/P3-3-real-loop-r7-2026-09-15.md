# P3-3 real 轮（30205）补测交付表（2026-09-15）

- 执行记录：P3-3-IMPL-20260907 补测会话（操作单
  `P3-3-evidence-recovery-2026-09-15.md` §4 获批后执行 real 判据轮；
  toy 轮同日先行完成，见 `P3-3-toy-loop-r7-2026-09-15.md`）
- 轮次：第十五轮（real 判据轮，操作单 §4.2 第二轮；**两轮补测至此全部
  完成，操作单 §4.5 限额 2/2 用满**）
- 范围声明：本表为实现侧事实登记，供独立验收会话复核；**本会话不
  自验收、不改 v7 独立报告与终态矩阵结论**（v7 矩阵中 C-REAL-LOOP
  保持 EVIDENCE_GAP，由独立会话以后继轮裁定是否采信本轮证据）。

## 1. 资产与执行身份

| 项 | 值 |
| --- | --- |
| 起点（设备） | 3.2.4 / vcode 30204 / imageSha `43faf0ca…`（完整 64 位见服务日志 13:41:13.459 请求参数，与 toy 轮终点 targetImageSha256 全等——两轮连续，同一设备同一 App 实例） |
| 目标资产 | 3.2.5 / vcode 30205 / targetSha `ab8528ecb8c4bc756dc2d12fddeb2eb454fa3c91472129f1881edecdff2bcfb4` / asset `32577aa5…` 284,590B（service_config `real-30205`，启动横幅 fail-closed 核验通过） |
| App | `com.wen.gaia.gaia.p33verify` 86/1.0.60（与 toy 轮同一 APK，85ce64df…；pid 24733 跨轮未重启——monoUs 从 toy 轮 345M 单调延续至本轮 7.7G 无跳变） |
| 服务/隧道 | python PID 24332（13:33:25 起，active-release real-30205，:8443）/ cloudflared PID 20712（域名 `checkout-sheep-full-evening.trycloudflare.com` 两轮未变） |
| 升级次数 | 1 次（real 轮主测；操作单 §4.5 限额内，两轮合计 2/2） |

## 2. 六状态证据表（核心交付）

| # | 判据 | 状态 | 原始文件及 SHA-256 | 实际观测 | 直接证据或推导 | 剩余缺口 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 会话开始设备身份+目标资产身份 | ✔ 直接 | `service-v7-real-20260915.log`（2,116B，SHA 见 SHA256SUMS-real） | 13:41:13.459 latest 请求参数 `currentVersionCode=30204&currentImageSha=43faf0cac092f54d…f3fc13`（完整 64 位）→200 1219B updateAvailable（30205/3.2.5/target `ab8528ec…`） | 参数即 App GET_INFO 读到的设备身份；与 toy 轮终点（RTT TEST_BOOT confirmed vcode=30204 + MONO_REBOOT_VERIFIED）全等连续 | — |
| 2 | App 对下载整包的实际校验结果 | ◐ 推导 | 同上服务日志；`logcat-live-real.log`（17,964,197B，全量，SHA 见 SHA256SUMS-real） | 13:41:16.419 download `asset-full-30205` →200 284,590B；13:41:22.794 `MONO_BUDGET_START` | 控制流推导：`MONO_BUDGET_START` 必经 `verifyFileMatchesAsset` 通过（ota_download.dart:254-284 fail-closed）；服务端发出 284,590B 与 asset 全等，App 发送总量 284,590 全等 | 无「校验通过」字面打点（与 toy 轮同构缺口） |
| 3 | durableOff 达到 totalLen | ✔ 直接 | `logcat-decisive-real.txt`（决定性子集） | 76 行 OTA_MONO：`MONO_BUDGET_RESET` durable=4096→284,590 每 4,096B 递进（70 行），末次 13:45:28.897 durable=284,590 | `MONO_END_ACK_OK durable=284590`（13:45:29.078）前置 fail-closed 双校验（statusOk 且 durableOff==total）——打点即设备持久位点已到 total | — |
| 4 | App 解析 ACK_END/OK 且关联本次会话 | ✔ 直接 | 同上 logcat | 13:45:28.582 `OTA sent 284590/284590` → 13:45:29.078 `MONO_END_ACK_OK durable=284590`（monoUs=7,668,803,015） | 同进程（pid 24733）同会话单调钟：monoUs 自 13:41:22 的 7,422,519,507 连续递增至 7,668,803,015，且跨轮自 toy 轮 345M 单调延续（App 未重启，两轮同一实例） | — |
| 5 | 重启后新连接 GET_INFO 返回目标身份 | ✔ 直接 | 同上 logcat；`rtt-device-real.log`（351B）；服务日志 | 13:45:29 BLE 重连失败（设备已 reset）；RTT `BLEACT: ok apply=15912ms stage=13572ms vcode=30205`→`reset`→第二段 `TEST_BOOT confirmed vcode=30205`；13:46:32.251 `MONO_REBOOT_VERIFIED` | `MONO_REBOOT_VERIFIED` 唯一产生路径 = `_probeTargetIdentity` 新连接 GET_INFO 全等比对（currentVersionCode==30205 且 currentImageSha==`ab8528ec…`）→ targetVerified；服务日志 13:41:16 后零请求，排除「手动检查更新」反例 | — |
| 6 | 原升级调用成功完成（completed） | ✔ 直接 | 同上 logcat；`real-success-screenshot.jpg`（510,748B，相册原件 adb pull）；`real-success-screenshot-user-forward.jpg`（108,966B，用户飞书转发版） | `MONO_REBOOT_VERIFIED` 即 targetVerified 分支内打点，必经 `_phase=completed` + return true（ota_service.dart:940-1000）；用户截图（13:49:56 拍摄）显示「固件升级成功 固件版本 3.2.4→3.2.5 升级耗时 5分11秒」 | — |

截图一致性交叉验证：截图 UI 耗时「5分11秒」与 logcat 时间线
（13:41:22.794 BUDGET_START → 13:46:32.251 REBOOT_VERIFIED = 5 分 09.5 秒）
吻合（±2s 为 UI 起止口径差）。

## 3. 三方时间线（logcat ↔ 服务日志 ↔ 设备 RTT）

| 时刻 | 通道 | 事件 |
| --- | --- | --- |
| 13:33:25 | 宿主 | 服务切 real-30205（PID 24332），五 fixture fail-closed 核验通过 |
| 13:33~34 | 宿主 | 预检 latest ×2（本机+公网，requestId `be01dadd…`/`ae49ba66…`，30204 参数均发现 30205，target/asset SHA 全等） |
| 13:35 | 宿主 | logcat 采集启动（`-v time` 不清 buffer） |
| 13:36 | 宿主 | RTT 签名验证（0x200540AC = `53 45 47 47 45 52 20 52 54 54`）+ JLinkRTTLogger 启动（只读） |
| 13:41:13.459 | 服务 | latest（参数 30204+`43faf0ca…` 完整值）→200 发现 30205（req `89a060a1…`） |
| 13:41:16.419 | 服务 | download `asset-full-30205` →200 284,590B（req `59911dd0…`）；**此后服务零请求** |
| 13:41:22.794 | App | MONO_BUDGET_START（用户点升级） |
| 13:41:23.111 | App | `OTA sent 128/284590`（首包） |
| 13:41~13:45 | App | MONO_BUDGET_RESET 每 4,096B 递进 durable=4096→284,590 |
| 13:45:28.582 | App | `OTA sent 284590/284590` |
| 13:45:29.078 | App | `MONO_END_ACK_OK durable=284590` |
| ~13:45:29 | App | BLE 重连失败（设备已复位，与 toy 轮同构） |
| ~13:45:29 | 设备 | `BLEACT: ok apply=15912ms stage=13572ms vcode=30205`→`BLEACT: reset` |
| ~13:45:30 | 设备 | 第二段 boot：`TEST_BOOT confirmed vcode=30205` |
| 13:46:32.251 | App | `MONO_REBOOT_VERIFIED`（新连接 GET_INFO 全等比对通过，距 END_ACK_OK 63.2s） |
| 13:49:56 | 用户 | 手机相册截图（终态 UI 升级成功 3.2.5） |

## 4. 与 toy 轮的连续性（同构性加强）

- 同一 APK（`.p33verify` 85ce64df…）、同一 App 进程（pid 24733，monoUs
  toy 轮 22M→345M → real 轮 7.4G→7.7G 单调连续）、同一服务/隧道
  （域名未变）、同一 J-Link RTT 地址（0x200540AC，同源镜像）。
- toy 轮终点（30204+`43faf0ca…`）= 本轮起点参数全等——两轮构成
  同设备连续两次独立 OTA 闭环，各自六状态证据链同构闭合。
- 传输速率同量级：toy 284,581B/4m19s，real 284,590B/4m06s。

## 5. 归档清单（`docs/ota-exec-notes/real-loop-r7-evidence/`）

见 `SHA256SUMS-real.txt`：决定性 logcat 子集（76 OTA_MONO + sent 首尾
+ BLE 失败行）、RTT 全文（351B）、服务日志全文（含 2 条预检行与 2 条
真实请求行，按时间戳/requestId 可区分）、成功截图×2（adb pull 相册
原件 510,748B + 用户飞书转发版 108,966B）。17.9MB logcat 全量与
SHA 登记后留 `.cache/p33-verify-r7/` 原地不拷贝。

截图原件带手机相册厂商尾标（`cameralbum!`，与 toy 轮原件同构），
头为合法 JPEG SOI；已弃用 `adb shell cat` 通道（toy 轮教训），
本轮直接 `adb pull`，字节完整。

## 6. 边界与写入审计

- 本轮全部写入位于 worktree `p3-3-admission` 内（`docs/ota-exec-notes/`、
  `.cache/`、看板回写）；零烧录、零 BCB 写入、零复位命令（设备走自然
  状态机，RTT 仅只读采集，采集前 mem8 验证签名）。
- 未动：p33acceptance 在装包与生产包、v7 冻结包与终态矩阵。
- 采集进程收尾：logcat 与 RTT logger 已停并核验零残留（adb/JLink
  进程已清理）。**服务（PID 24332）与隧道（PID 20712）待停**——
  归档回写完成后按操作单 §4.6 清理（见 §7）。

## 7. 剩余工作

1. **清理**：停服务 PID 24332 与隧道 PID 20712、核验端口/进程零残留
   （操作单 §4.6 范围，本会话执行）。
2. **v8 重冻结**：日志增强提交 253f153 + toy/real 两轮证据已齐，
   后继合同 v8（新 freeze_commit/新 APK/新目标身份如实登记）由独立
   会话以 v7 终态矩阵为前驱安排。
3. 两轮证据采信与 C-TOY-LOOP/C-REAL-LOOP 判定：独立验收会话职权，
   本会话不预判。
