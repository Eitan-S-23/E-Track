# P3-3 v8 E01 用户确认与处置（2026-09-15）

## 用户原话

本线程在 v8 独立验收报告交付后，用户明确回复：

> 旧包是我卸载的，接下来应该怎么做？

本确认只涉及旧受验包 `com.wen.gaia.gaia.p33acceptance` 的卸载来源。
11:35:31 的卸载时间来自已归档原始日志，不伪装成用户另行提供的时间。

## 独立裁定

E01 的操作来源疑点关闭：旧包由用户自主卸载，不属于实现 agent 或验收
agent 的越权操作，也不是产品缺陷。原始“旧包已不存在”的观测保持不变，
不改写成旧包仍在装，也不追认 agent 获得卸载或清数据权限。

本确认不替代新受验 APK 的实物取证。新 `.p33verify` 包的安装、大小、
SHA-256、签名证书，以及生产包未变的事实，已由 v8 R1 独立观测证明。
两条 OTA 闭环和 CI/构建的四项 PASS 不受影响。无需新构建、升级、烧录、
J-Link、服务部署或重采旧日志。

## 后续范围

保留 v8 R1 的 EVIDENCE_GAP 原报告和终态矩阵，作为当时尚缺本确认的真实
记录。本文件补足唯一待决事实，不冒充已完成终态矩阵更新或机器复校。
下一步仅办理最终验收报告/矩阵的可追溯归档及校验，再进行 Git 收口。

用户此次回复确认了事实，未授权 git commit/push/merge 或 worktree 删除。
这些动作仍须集中取得用户确认；本次未执行。
