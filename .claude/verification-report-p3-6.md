# 质量审查报告：P3-6「BLE 帧层测试 CI 接线」独立验收

- 审查对象：看板卡 P3-6 的实现交付 + 本轮验收动作本身
- 审查角色：独立验收会话（非实现方）
- 审查时间戳：2026-09-02
- 审查方法：sequential-thinking 深度分析 + 合同制 fail-closed 校验（`validate_bundle.py`）
- 单一事实源：哈希、数值、命令输出全部落在
  `docs/ota-exec-notes/P3-6-acceptance-report.md`，本文件只做审查判断，不复制指纹
  （遵循内容唯一性规则：引用而非复制）。

---

## 1. 审查清单

### 1.1 需求字段完整性

| 字段 | 状态 | 依据 |
| --- | --- | --- |
| 目标 | ✅ 明确 | 看板 P3-6 卡「把两套 BLE host 测试接入 firmware-build.yml 测试步骤」 |
| 范围 | ✅ 明确 | 卡内「范围」限定 workflow 测试步骤，不含产品代码 |
| 交付物 | ✅ 明确 | workflow 改动 + CI 运行证据 + fail-closed 反证 |
| 审查要点 | ✅ 明确 | 卡内验收字段 = 接线 + fail-closed 可反证 |

冻结任务源为「看板 P3-6 卡验收字段 + 派工书 `docs/ota-prompts/prompt-P3-6-implementation.md`
完成判据」。合同 13 条判据全部可回溯到该任务源，`gate.basis` 均为 `frozen_requirement`，
**无一条为验收会话自拟门槛**——这是 P3-1-v1 曾踩过的坑（自拟 `disconnect_resume=10/10`
并在 v2 删除），本轮刻意避免。

### 1.2 覆盖原始意图无遗漏或歧义

原始意图可拆为三问，逐一有判据回答：

1. **接线了吗？** → C01（命令序列逐行全等）、C02（纯 2 行增量、触发段未动）。
2. **接的是真测试还是摆设？** → C05（CI 绿运行里两个 PASS 标记各 1 次，证明确实跑过）、
   C11（本地复算 `checks/failures` 与 CI 逐字一致，证明没偷换弱化版）。
3. **失败会红吗？** → C03（六类吞码模式零命中）、C06（真实红运行 + 步骤级归因 +
   会话层因 pipefail 短路无输出）、C07（同提交对照运行仍绿，排除伪鉴别力）、
   C08（还原后恢复绿，反证链闭合）。

第 3 问的处置是本轮最值得肯定之处：**没有停在「静态检查没有 `|| true`」这一层**，
而是用真实注错运行取得动态反证，并追加同提交对照运行排除「该提交让所有流水线一起红」
的伪鉴别力。这正是规约禁止的「用没有错误日志推定通过」的反面。

### 1.3 交付物映射明确

| 交付物类别 | 落盘位置 |
| --- | --- |
| 产品改动 | `.github/workflows/firmware-build.yml`（+2 行） |
| 实现方证据 | `docs/ota-exec-notes/P3-6-ci-wiring-evidence.md` |
| 治理记录 | `docs/ota-exec-notes/P3-6-governance-change.md`、看板 P3-6 卡与 §10 会话日志 |
| 验收 harness | `tests/ota/p3_6_verify_{ci_wiring,ci_evidence,scope,harness}.py` |
| 验收合同 | `docs/acceptance-contracts/P3-6-v1.contract.json` |
| 证据包 | `docs/acceptance-contracts/P3-6-v1/`（命令日志 / CI 采集件 / 产物 / 3 组 manifest） |
| 验收报告 | `docs/ota-exec-notes/P3-6-acceptance-report.md` |
| 审查报告 | 本文件 |

### 1.4 依赖与风险评估

| 项 | 评估 |
| --- | --- |
| 上游依赖 | P3-1（两套 BLE 测试已入库）已满足：C09 核实 4 个文件均在 `main` 跟踪 |
| 下游影响 | `firmware-build.yml` 每次触发多跑 2 条 host 测试，秒级开销，不影响构建产物 |
| 冻结资产风险 | 本卡改 Production `top_file`，使 P3-1-v2 Production 指纹漂移 → DEV-3 如实声明、不回改 |
| 收口风险 | 注错提交 `7b58ed6` 若以 merge 方式并入将成为 `main` 可达历史 → 已登记为不阻断发现 2 |
| 验收自污染风险 | 合同/矩阵/报告落在无 profile 枚举的路径；7 条命令实跑后 `git status` 未变 |

### 1.5 审查结论已留痕

本文件 + 验收报告 §10 + 看板 P3-6 卡状态回写 + 看板 §10 会话日志各一处，均含 2026-09-02
时间戳与轮次标识 `P3-6-V1-FREEZE-20260902-01`。看板按规约**不记录任何哈希**
（含 commit SHA），避免 Governance `top_file` 自指环。

---

## 2. 技术维度评分：48 / 50

| 子项 | 分值 | 依据与扣分理由 |
| --- | --- | --- |
| 接线正确性 | 20/20 | 命令序列逐行全等（1 守卫 + 6 基线 + 2 新增）；`git diff --numstat` 为 2 增 0 删；触发段 57 行与 base 字节一致；workflow 全文无 `p3_1_verify` 之类验收脚本混入 |
| fail-closed 有效性 | 18/20 | 静态（六类吞码模式零命中）+ 动态（真实红运行、步骤级归因、会话层短路无输出）+ 对照（同提交治理流水线仍绿）三重证据。**扣 2 分**：步骤名仍为 `Test Boot fw_header validator vectors`，CI 失败时归因可读性下降 |
| 测试有效性 | 10/10 | C11 本地复算与 CI 标记逐字一致，同时排除「弱化版测试」与「环境特有产物」两种可能 |

**代码质量**：本卡产品改动仅 2 行 YAML，追加在既有命令序列末尾，与既有 6 条风格一致
（同为 `python3 tests/ota/<name>.py`），无新增缩进层级、无 shell 技巧、无平台特定写法。
符合「遵循既有代码风格」与「不引入自研方案」。

**测试覆盖**：本卡自身的验收 harness 接受了变异测试（C12：3 组对照 + 15 处单点变异，
每例绑定预期失败原因并核对四件事），排除了常量 PASS 与「恒失败」两种伪鉴别力。
这满足规约「harness 必须 fail-closed，禁止常量 PASS、无条件汇总字段」。

**规范遵循**：
- ✅ 未改 `Libraries/OTA/**` 等 20 条产品红线（C09 零 diff）
- ✅ 未削弱 fail-closed 语义（C03）
- ✅ 未改 P3-1 冻结资产（DEV-3 声明漂移而非回改）
- ✅ 未把 AC5 产物用于 OTA/CI
- ✅ 中文文档、UTF-8 无 BOM、`.ps1` 无涉及

---

## 3. 战略维度评分：46 / 50

| 子项 | 分值 | 依据与扣分理由 |
| --- | --- | --- |
| 需求匹配 | 20/20 | 13 条判据全部回溯到冻结任务源，无自拟门槛；未越卡改产品代码；`scope.not_observed` 为空（无「不观测」项） |
| 架构一致 | 18/20 | 复用既有测试步骤而非新建 job/workflow，复用 `python3 tests/ota/*.py` 既有调用约定，复用 P3-1-v2 的合同/矩阵/manifest 结构与 `external_inputs` 先例。**扣 2 分**：注错提交仍在分支历史，收口方式若选错会把 CRC 缺陷带入 `main` 可达历史 |
| 风险评估 | 8/10 | 5 条授权偏离均写明成因、处置与证据；两条不阻断发现均给出定性与建议。**扣 2 分**：DEV-3 导致仓库内同时存在两份对同一 `top_file` 的不同 Production 指纹记录，后续读者须依赖验收报告才能理解成因，属治理可读性代价 |

**需求匹配的关键判断**：P3-1-v2 曾把「workflow 未接入两套 BLE 测试」列入
`excluded_by_freeze` 并新立 P3-6 承接。本轮验收确认该承接**已闭环**：
P3-6 的 C01–C08 正是 P3-1-v2 当时排除的那一项，且未借机扩大为「顺便改步骤名」
「顺便调触发条件」等范围外改动（C02 的 `added=2 removed=0` 与触发段字节一致即为证明）。

**架构一致的关键判断**：本卡没有为「CI 接线」新造机制——没有新 workflow、没有新 job、
没有新脚本调用约定、没有为验收另开旁路。这符合「标准化 + 生态复用拥有最高优先级」。

---

## 4. 综合评分与建议

**综合评分：94 / 100**
**明确建议：通过**

依据全局准则决策规则：综合评分 ≥ 90 分且建议「通过」 → **确认通过**。

单轮验收结果：**PASS**（取值域 `{PASS, PRODUCT_FAIL, HARNESS_FAIL, EVIDENCE_GAP,
ENV_BLOCKED}`）。任务状态与单轮结果分离；验收会话**未覆盖实现认领字段**，
看板 P3-6 卡的认领标识仍为实现方。

---

## 5. 本轮验收自身的可信度自查

规约要求验收不得自证，故对本轮动作本身也做一遍反向检查：

| 自查项 | 结论 |
| --- | --- |
| 验收者是否为实现者？ | 否。实现方为 `Claude(实现会话)`，本轮为非实现独立会话 |
| observed 是否从门槛反抄？ | 否。矩阵生成器从包内日志逐元素正则派生后再与门槛断言全等，派生不出即抛错 |
| 是否存在常量 PASS？ | 否。C12 用 3 组对照 + 15 处单点变异证明 harness 会红 |
| 是否用「没有错误日志」推定通过？ | 否。每条判据绑定具体打印行的具体数值 |
| 是否存在证据复用自证？ | 否。13 条全 `EXECUTED`，`reused_from_round` 全 `null`，无 `REUSED` |
| 门槛是否为不变量？ | 是。`state_chain` 用列表全等比较，故刻意排除随工作树变化的计数（DEV-4），并在合同落盘后复跑实测确认门槛仍成立 |
| 是否存在指纹自指环？ | 否。合同/矩阵/报告落在无 profile 枚举的路径；看板不含任何哈希 |
| 校验器是否真跑？ | 是。`VALIDATION=PASS`，`--repo-root` 为本轮精确 worktree，校验器重新枚举并读取真实文件 |
| 性能门槛是否反向冻结？ | 不适用。`performance_gates` 为空，本卡无性能判据 |

**唯一残留的方法学局限**：CI 侧结论取自冻结的 `gh` 采集件而非本轮重跑。这是按
`scope.excluded_by_freeze` 第 1 条的冻结口径处置——卡的验收字段不要求验收方重跑流水线，
按规约不得事后追加为门槛。采集件已按字节冻结并绑定 SHA-256，
`p3_6_verify_ci_evidence.py` 对其做 88 项机器核对，可复算。

---

## 6. 移交事项

1. **不阻断发现 2 条**（步骤名漂移、注错提交仍在分支历史）详见验收报告 §8，
   均给出定性与建议，不影响本轮 PASS。
2. **收口待办**详见验收报告 §11。按 `AGENTS.md` §「OTA 执行规约」第 5 条，
   验收会话不执行 `git commit/push/merge`，收口由主会话在用户确认后小步进行，
   并须完成 §「Git / Worktree 收口规约」的 `--ff-only` 同步闭环。
3. **建议合并方式**：squash-merge（使含 CRC 缺陷的 `7b58ed6` 不进入 `main` 可达历史）。
   若保留提交粒度，须在收口证据中显式登记该提交为「刻意注错、已还原、禁止单独检出使用」。
