# PLAN-OTA-GUIDE.md — OTA 执行驾驶手册(用户视角)

> 配套 `PLAN-OTA-EXEC.md`(看板)使用。agent 的行为规则在 `AGENTS.md` "OTA 执行规约"里,会随会话自动生效;**本手册只回答一件事:你在每个阶段说什么、亲手做什么、看什么才放行**。

## 0. 角色分工(一句话)

- **你**:派单、审阅放行、完成五类人工点(提交确认 / Secrets / 手机操作 / 物理断电 / SD 卡拷文件);
- **执行 agent**:认领卡、干活、附证据、回写看板;
- **主会话(Claude)**:可以当调度器替你逐卡派发与验收,只在人工点找你(最省心,推荐日常用)。

术语提醒:"认领"是 agent 做的动作,你做的动作叫"派发"——把话术发给它即可。

## 1. 六句常用话术(复制即用)

| 场景 | 话术模板 |
|---|---|
| 单卡派发 | 按 PLAN-OTA-EXEC.md 认领 **P0-1** 执行 |
| 批量派发 | 按 PLAN-OTA-EXEC.md 从 **PRE-1** 顺序执行到 **PRE-3**,每卡完成先回写看板再开下一张,遇阻塞停 |
| 主会话调度 | 按看板继续推进,到需要我的停点为止 |
| 查进度 | 读 PLAN-OTA-EXEC.md,报告各阶段状态与阻塞项 |
| 验收派发 | 对 **P0-4** 做验收:按卡内验收标准执行,通过则在证据栏追加验收行,不通过打回置"进行中"并说明原因 |
| 提交收口 | 把 **PRE** 阶段的改动按小步提交收口(这句话本身就是提交授权) |

加粗处换成实际卡号/阶段号即可。任何 agent(包括 Codex)收到都按同一套规约办,因为规则在 AGENTS.md 里。

## 2. 每张卡的标准回合(你只做三件事)

1. **派发**(上表话术);
2. agent 回报后**看两处**:打开看板找到该卡——"证据"栏是否非空、证据与结论是否相符(命令输出/路径/哈希是实的,不是一句"已完成");
3. **派验收**给另一个会话(验收分离是规约要求;主会话调度模式下我自动安排)。

阶段末追加第四件:说"提交收口"。
若 agent 回报完成但看板没更新,回它一句:**"先按规约回写看板再报告"**。

## 3. 分阶段操作指南

### PRE 前置修正(4 卡,~1d)

- **开工条件**:无,现在就能发。
- **推荐派法**:`PRE-1→PRE-3` 批量派发(都是文档/workflow 修订,风险低);`PRE-4` 单独走。
- **你亲手做**:
  1. PRE-1/2 完成后**过目 PLAN-OTA.md 的修订 diff**(契约变更必须你点头,让 agent 把 diff 摘要发你);
  2. PRE-4:agent 会报告 vendor/ 体积,你确认后说"提交收口",推送后去 GitHub 看 Actions 是否绿。
- **放行判定**:4 卡全"完成",Actions 干净构建绿 → 可开 P0。

### P0 契约冻结+基建(6 卡,3d)——全项目根基,最值得你花时间的阶段

- **开工条件**:PRE-1/2/3 完成。
- **推荐派法**:`P0-1` **单卡**(契约文档,产出后建议你亲自通读一遍字段表——之后一切实现以它为准,这里出错成本最高);`P0-2→P0-3` 批量;`P0-4`、`P0-5` 单卡(真机压测 1000 次,耗时长,agent 用 J-Link 自动跑);`P0-6` 单卡。
- **你亲手做**:
  1. 通读 `docs/ota-binary-contracts.md`(至少过字段表和状态码);
  2. 真机压测期间保持设备连着 J-Link 别动,**关闭 RTT Viewer**(它和采集会互抢数据);
  3. 阶段末让 agent 出"R4 五条+R8 五条落文对照表",你核一眼。
- **放行判定**:6 卡全绿 + vectors 三方一致 + 压测零错 + R4 五条全落文。**这是最重要的一次放行:P0 不完,P1/P2 禁止开工**(方案硬门槛)。

### P1 bootloader(6 卡,5-7d)——设备会反复"变砖再救活",属预期

- **开工条件**:P0 全部完成。
- **推荐派法**:`P1-1→P1-4` 逐卡或两卡一批;`P1-5` 单卡;`P1-6` 单卡且约好你在场的时间。
- **你亲手做**:
  1. `P1-5` bootstrap 执行前确认一次:这一步之后设备永久切到 boot+app 新布局(J-Link 在手随时能救回,但要知情);
  2. `P1-6` 断电矩阵:agent 会先自动跑完 J-Link 复位可近似的点,然后给你一张"物理断电点位清单"(什么时机拔电/上电),你照单配合,每点几分钟。
- **放行判定**:注错试验(候选 CRC 坏/搬运断电/三连失败)全回滚 + 断电矩阵 20 点全绿。

### P2 MCU App 升级链(6 卡,4-6d)

- **开工条件**:P0 完成(编码可与 P1 并行;但 `P2-5` 的真机闭环验收需要 P1 的 boot 已在设备上)。
- **推荐派法**:`P2-1→P2-3` 逐卡;`P2-4`(SD 文件管理页)单卡,agent 会出模拟器截图;`P2-5` 大验收单卡;`P2-6` 收尾。
- **你亲手做**:
  1. 看 `P2-4` 的模拟器截图,确认 UI 可接受;
  2. `P2-5` 真机 SD 闭环:**把 agent 打好的测试 .etu 拷入 SD 卡**(拔卡→电脑拷贝→插回设备),然后按提示在设备上操作"文件管理→选包→确认升级"并重启。
- **放行判定**:P0 打包器产物走通真机 SD 升级全程(STAGED→重启→APPLYING→TEST_BOOT→CONFIRMED,RTT 全程留痕)。

### P3 BLE+Flutter(5 卡,5-7d)

- **开工条件**:P2-1/2 完成。
- **推荐派法**:`P3-1→P3-2` 逐卡;`P3-3` 单卡(APK 由 GitHub Actions 构建,本地禁构建);`P3-4` 自动;`P3-5` 约好你的时间。
- **你亲手做**:
  1. `P3-3` 完成后从 Actions 下载 APK 装到手机;
  2. `P3-5`:按 agent 指引操作 APP——连接设备、发起升级、**中途人为制造断连**(走远/关蓝牙)共 10 次验证续传;期间关闭 RTT Viewer。
- **放行判定**:手机→设备完整 BLE 升级闭环 + 断连×10 续传全过 + 升级后 GET_INFO 显示新版本。

### P4 CI/CF(4 卡,2-3d)——可与 P1/P2 并行穿插

- **开工条件**:P0 完成。
- **推荐派法**:`P4-1→P4-3` 批量派发(纯 workflow/SQL/文案);`P4-4` 是你的活。
- **你亲手做**(`P4-4`,agent 会给逐项步骤清单):
  1. 在 GitHub 配 Secrets:`CLOUDFLARE_ACCOUNT_ID`、`CLOUDFLARE_API_TOKEN`、`TRACE_UPDATE_SERVICE_URL`、`TRACE_DEPLOY_TOKEN`、R2 bucket 变量、AES 生产 key;
  2. 建 `firmware-production` environment 并把自己设为审批人;
  3. `P4-1` 演练时在 Actions 页面点一次人工审批。
- **放行判定**:workflow_dispatch 正式发布演练全绿(三资产+注册成功)+ D1 约束 SQL 测试全过("插第二个 full 被拒")。

### P5 联调验收(3 卡,3-5d)

- **开工条件**:P1-P4 全部完成。
- **推荐派法**:`P5-1` 单卡;`P5-2` 分两段(自动段 agent 自跑,物理段约你);`P5-3` 收尾。
- **你亲手做**:配合物理断电/拔插卡抽样点;手机侧配合弱信号(走远)、低电量场景;最后和 agent 一起过一遍风险台账,确认 §9 开口项全部关闭或明确转 v2。
- **放行判定**:双通道 4/4、故障矩阵全绿 → **项目完成,可正式发布**。

## 4. 异常处理三则

1. **看到"阻塞"**:打开看板 §9 变更登记表看冲突描述,想清楚后回复:"针对 §9 登记的 XX 冲突,我的决定是……,按此修订契约并解除阻塞"。契约怎么改永远由你拍板,agent 无权自改。
2. **同一问题连续失败三次**:直接说"暂停,回到设计复盘"(全局准则,防止 agent 越陷越深)。
3. **状态存疑/换了 agent/隔了几天**:任何时候发"读 PLAN-OTA-EXEC.md,报告各阶段状态与阻塞项"——状态在文件里,谁来都能接。

## 5. 一页速查表

| 阶段 | 启动话术 | 你的人工点 | 放行判定 |
|---|---|---|---|
| PRE | 批量:PRE-1→PRE-3;然后单发 PRE-4 | 审 diff、确认提交 | 4 卡完成+Actions 绿 |
| P0 | 单发 P0-1;批量 P0-2→P0-3;单发 P0-4/5/6 | 通读契约文档、关 RTT Viewer | 6 卡+压测零错+R4 落文 |
| P1 | 逐卡 P1-1→P1-5;约时间发 P1-6 | bootstrap 知情确认、物理断电配合 | 注错全回滚+矩阵 20 点 |
| P2 | 逐卡 P2-1→P2-4;单发 P2-5/6 | 看截图、SD 卡拷 .etu | 真机 SD 闭环全程 |
| P3 | 逐卡 P3-1→P3-4;约时间发 P3-5 | 装 APK、操作手机断连×10 | BLE 闭环+续传 10/10 |
| P4 | 批量 P4-1→P4-3;P4-4 自己做 | 配 Secrets、点审批 | 发布演练绿+SQL 约束过 |
| P5 | 单发 P5-1/2/3 | 物理配合、过风险台账 | 矩阵全绿→发布 |

**最省心的替代方案**:以上派单动作全部可以换成对主会话说一句"按看板继续推进,到需要我的停点为止"——我来逐卡派发、验收、回写,只在上表"你的人工点"一列出现时找你。
